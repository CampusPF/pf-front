/* TODO(campus): progreso hardcodeado sólo para que el player y la CTA se vean
   con estado. Cuando entren auth + backend esto sale de la sesión del usuario. */

/** Lecciones marcadas como vistas (atraviesa todos los cursos, los ids son únicos). */
export const MOCK_COMPLETED_LESSON_IDS = ["l1", "l2", "l3", "l15", "l16"];

/** Porcentaje de avance que muestra el header del player. */
export const MOCK_PROGRESS_PERCENT = 24;
